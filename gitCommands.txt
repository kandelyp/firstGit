1. $git init // initialie local Git Repository

2. $git add <file> // add file(s) to index

 	*.format adds all files with that format
       .  adds all files in the current directory to staging area

3. $git status // check status of working tree

4. $git commit // commit changes in index
	it this command line is used without option, default editor , I guess. make changes and save.
	
	to add comment without using anyother editor, use
		git commint -m 'message'
5. $git push  // push to remote repository

6. $ git pull //pull latest from remote repository

7. $git close // clone repository into a new directory


8. add name and email
	$git config --global user.name 'Yaad'
	$git config --global user.email 'kandelypg@gmail.com'


